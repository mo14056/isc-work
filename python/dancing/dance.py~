"""
dance.py
========

Contains the infamous "boogie" function used to generate fantastic
fun at the disco.

"""

# Standard library imports
import time
import random

# Define some dance moves
moves = ["Mash Potato", "Hippy Hippy Shake", "Back flip", "Cha Cha Cha",
         "Mosh", "Slam Dive", "Waltz"]

def boogie(dance_moves):
    """
    This programme dances!

    """
#3 quotation marks work like the # but appear when running the program

    these_moves = moves + dance_moves #when we add anything after the python dancer.py it adds it at the very end of the list of moves that is already created.

    print ' these_moves are ', these_moves #prints the list of moves

    for count in range(1, 5):
        print count #prints the number for the specific count
        time.sleep(0.3) #importa la funcion dormir desde la libreria q para el bucle momentaneamente durante un tiempo especifico

    print "GO!"
    for i in range(50):
        time.sleep(0.3)
        index = random.randint(0, len(these_moves) - 1) #la variable index se selecciona de manera aleatoria entre el primer y ultimo valor del rango
        print these_moves[index] #prints the move



